<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="Peluditos.com">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SearchSeguimientos"></activity>
        <activity android:name=".SearchDenunciaActivity" />
        <activity android:name=".SearchUsuarioActivity" />
        <activity android:name=".SearchmascotaPActivity" />
        <activity android:name=".DenunciasActivity" />
        <activity android:name=".Add_mascotaperdidaActivity" />
        <activity android:name=".Mascotas_perdidas" />
        <activity android:name=".RegistroActivity" />
        <activity android:name=".menu" />
        <activity android:name=".InicioSesion" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>